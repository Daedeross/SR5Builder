<UserControl x:Class="SR5Builder.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xtk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:SR5Builder.Views"
             xmlns:proj="clr-namespace:SR5Builder"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="methodList" MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="proj:CharGenMethod"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <Grid>
        <ListView ItemsSource="{Binding Properties}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Setting" DisplayMemberBinding="{Binding Key}" />
                    <GridViewColumn Header="Value">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding Value}">
                                    <ContentControl.Resources>
                                        <DataTemplate DataType="{x:Type sys:Int32}">
                                            <xtk:IntegerUpDown Value="{Binding}" />
                                        </DataTemplate>
                                        <DataTemplate DataType="{x:Type sys:Single}">
                                            <xtk:SingleUpDown Value="{Binding}" />
                                        </DataTemplate>
                                        <DataTemplate DataType="{x:Type proj:CharGenMethod}">
                                            <ComboBox ItemsSource="{Binding Source={StaticResource methodSource}}"
                                                      SelectedItem="{Binding Value}">                                                
                                            </ComboBox>
                                        </DataTemplate>
                                    </ContentControl.Resources>
                                </ContentControl>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
